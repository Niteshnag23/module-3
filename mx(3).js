const input=require("readline-sync")
let a= input.question("enter the number");
let b=input.question("enter the number");
let c=input.question("enter the value");
let p=Math.max(c, Math.max(a,b))
    console.log(p)

